{% extends 'base.html.twig' %}

{% block title %}User{% endblock %}

{% block body %}
    <div class="dashboardUserPage">
        <div class="welcomePicture">
        <div class="circle-profilPicture"></div>
    <h1 class="titleWelcomeUserPage">Bienvenue {{ user.firstname}}</h1>
        </div>
        <section class="buttons-userPage">
            <a href="{{ path('app_user_edit', {'id': user.id}) }}">
                <button type="button" class="btn btn-warning">Personal Informations</button>
            </a>
            <a href="{{ path('app_user_edit', {'id': user.id}) }}">
                <button type="button" class="btn btn-warning">Edit Picture</button>
            </a>
            <a href="{{ path('app_user_edit', {'id': user.id}) }}">
                {{ include('user/_delete_form.html.twig') }}
            </a>
        </section>
    </div>
    <img src="/assets/images/tipsPublished.png" id="tt-bannersUserPage">

    <img src="/assets/images/tipsSaved.png" id="tt-bannersUserPage">
{% endblock %}
